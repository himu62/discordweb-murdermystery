import { FC } from "react";
import { Control } from "react-hook-form";
import { Scenario } from "@/src/store";
import { Box } from "@mui/material";

type Props = {
  control: Control<Scenario>;
};

const ChannelEditor: FC<Props> = ({}) => {
  return <Box></Box>;
};
export default ChannelEditor;
